let os = require("os"); // operating system libraries.
console.log(os.arch()); // which architecture the operating system is based on?
console.log(os.platform()); // the platform we are using 
console.log(os.networkInterfaces()); // all information about connection and networks
console.log(os.epus()); // ram check
